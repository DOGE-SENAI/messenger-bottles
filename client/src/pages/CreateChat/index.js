import React from "react";
import { StyleSheet, Text } from "react-native";

const CreateChat = () => {
  return <Text>CreateChat</Text>;
};

const styles = StyleSheet.create({
  containerMain: {
    display: "flex",
    width: "100%",
    height: "100%",
    alignItems: "center",
    backgroundColor: "#fff",
  },
});

export default CreateChat;
